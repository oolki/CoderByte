function SimpleSymbols(str) { 
    if ((str.charAt(0) && str.charAt(str.length - 1)) === '+') {
        return true;
    }else{
        return false;
    }
  //create  a function that takes one string argument and tests
  //if the string is contained by addition symbols on both sides
  
         
}